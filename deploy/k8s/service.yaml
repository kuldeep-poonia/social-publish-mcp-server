apiVersion: v1
kind: Service
metadata:
  name: social-mcp-service
  namespace: social-mcp
  labels:
    app: social-mcp-server
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: social-mcp-server
